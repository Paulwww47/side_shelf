# 变更日志

本文件记录 Side Shelf 的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-19

首个正式版本。主程序为 C++20 + Qt 6 实现，旧 Python/PySide6 版保留在
`legacy_python/` 作为参考。

### 新增

- 常驻屏幕右侧的把手（药丸形侧边条）：鼠标移到屏幕最右边缘时滑出，
  居中显示当前暂存数量
- 点击把手暂存剪贴板中的**非文字**内容（文件、图片）；把文件或图片拖到
  把手上同样可以暂存
- 中键打开预览面板：图片按原始比例瀑布流排布，文件显示系统图标
- 预览面板内左键把条目平滑放大到原尺寸，其余条目在放大项右侧自上而下
  单列排布，可在列中带动画切换放大项
- 预览面板内右键删除条目，底部出现 5 秒撤销条，超时或关闭面板后提交删除
- 预览面板内中键用默认程序打开文件
- 把文件拖到上方小圆可把全部暂存内容作为文件拖出（复制，暂存保留）
- 从压缩软件 / 资源管理器 zip 视图拖出的临时文件会先复制到 `stash/`
  再登记，避免源程序清理临时目录后条目变成死路径
- 同一张图片的临时文件路径与位图按解码后像素判等，只保留一份
- 自动监测剪贴板（右键菜单开启，重启后默认关闭）
- 开机自启：写入 `HKCU\...\CurrentVersion\Run`，无需管理员权限
- 按住把手拖动可调整垂直位置，滚轮或菜单可调透明度，两者均被记住
- 有程序全屏时自动完全隐藏，退出全屏后恢复
- 单实例运行；已在运行时重复启动会静默退出

### 说明

- **退出即清空**：暂存内容只在本次运行内有效，不写入磁盘长期保存
- 清空与删除只清理本程序在 `stash/` 中的副本，**不会触碰用户的原始文件**
- 界面渲染优先使用 GPU（OpenGL 4× MSAA 离屏渲染 + 把手 3× 超采样），
  无可用 GPU（远程桌面 / 无驱动虚拟机）时回退到软件光栅 3× 超采样
- 界面文字统一为微软雅黑 Light；仅把手数量徽标用 Bold、右键菜单用 Regular
- 发布包内含全部 Qt 运行库，拷贝到其他 Windows 10/11 x64 电脑即可运行，
  无需安装 Python 或任何运行时

### 已知问题

- 构建目录的头文件依赖跟踪不可靠：**改动任何 `.h` 后必须干净重建**，
  否则会出现新旧头文件混编（对象 new/delete 尺寸不匹配 → 预览窗口打开即
  堆损坏崩溃）。详见 [CONTRIBUTING.md](CONTRIBUTING.md)
- 自检（`--selftest`）依赖真实交互式桌面会话，无法在无头 CI 上运行

[1.0.0]: https://github.com/Paulwww47/side_shelf/releases/tag/v1.0.0
